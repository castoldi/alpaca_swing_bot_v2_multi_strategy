[pytest]
testpaths = tests
addopts = -q
markers =
    real_signal_window: exercise bot._signal_is_actionable instead of the always-open test default
    protection_audit: run bot._audit_protection instead of the no-op test default
